syntax = "proto3";

package time;

option go_package = "https://github.com/frederikgantriis/grpcGolang.git;time";

service getCurrentTime {
    rpc getTime (GetTimeRequest) returns (GetTimeReply) {}
}

message GetTimeRequest {}

message GetTimeReply {
    string reply = 1;
}